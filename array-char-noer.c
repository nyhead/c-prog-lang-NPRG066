/*
 * Print 0..9 recursively using a function call fn(9).
 */
#include <stdio.h>

int
main(void)
{
	char foo[] = { 'b', 'a', 'r'};

    printf("%s\n", foo);	/* will print "bar" without the quotes */
}